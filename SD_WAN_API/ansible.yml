---
- name: cau hinh switch_1
  hosts: switch-01
  gather_facts: False
  tasks:
    - name: show ip
      ios_command:
        Commands:
          - sh vlan br
      register: Output

    - debug: var=Output.stdout_lines
